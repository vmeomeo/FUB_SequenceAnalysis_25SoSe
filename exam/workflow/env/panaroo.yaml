#WORKING almost
# name: panaroo_env
# channels:
#   - conda-forge
#   - bioconda
# dependencies:
#   - python=3.7
#   - panaroo
name: panaroo_env
channels:
  - conda-forge  # Higher priority for biopython
  - bioconda
dependencies:
  - python=3.10 #3.7
  - panaroo=1.5.2  # Keep unversioned to maintain working state
  - biopython=1.80 #1.76  # Add this line (version matches panaroo's requirements)
