name: vis
channels:
  - conda-forge
  - bioconda
  - defaults  # Ensures broader compatibility
dependencies:
  - python=3.12.*  # Must be ≤3.12 (3.11 also works)
  - ete3=3.1.3     # Latest stable version (or pin to 3.1.2 if needed)
  - xorg-libxrender  # Required for rendering
  - xorg-libxext     # Required for X11 support
  - xvfbwrapper      # For headless rendering